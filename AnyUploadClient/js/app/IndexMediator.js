function IndexMediator() {
    // 关心消息数组
    this.listNotificationInterests = [];
    // 关心的消息处理
    this.handleNotification = function (data) {
        switch (data.name) {

        }
    };
}
indexMediator = new IndexMediator();